import java.util.*;
import java.util.function.Function;
import java.util.stream.Collector;
import java.util.stream.Collectors;

/*public class StreamTest9 {

    public static void main(String[] args) {
        //List<String> list = new ArrayList<>();
        List<String> list = Arrays.asList("a", "bb", "ccc", "dd", "ccc", "bb", "bb");
        list.stream().collect(Collectors.groupingBy(Function.identity(), Collectors.counting()))
                .entrySet().stream().forEach(System.out::println);
                //.collect(Collectors.toMap(e -> e.getKey(), e -> e.getValue(), (e1,e2) -> e1.equals(e2), HashMap::new));
    }
    try{}catch(IOExceptio e)catch(ArrayIndexOutofException e) catch(Exception)

}*/
