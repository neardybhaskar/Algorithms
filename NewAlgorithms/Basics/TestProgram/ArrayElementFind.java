/*
import java.util.Arrays;

public class ArrayElementFind {

    public static void main(String[] args) {
        int[] ar = new int[] {1,2,3,5,6,7,8,9};
        Arrays.sort(ar);
        int count = ar[0];
        try {
        for(int i=0;i<ar.length;i++) {
            if(ar[i] != count) {
                System.out.println(count);
                count++;
            }
            count++;
        }

        //Employee in finance department with 2nd highest salary
        //Employee eid did salary
        //Department did d_name
        //select e.e_name, e.salary from employee e inner join department d on e.did == d.did where d.d_name = "finance"
        //Order by salary DESC LIMIT 1 OFFSET 1;

        //select max(sal) from employye where sal not in (select max(sal) from employee )
    }

}
*/
